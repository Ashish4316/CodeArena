After adding tailwind dependencies run:

npm install

Then run dev server:

npm run dev

If PostCSS errors related to tailwind appear, ensure 'postcss.config.cjs' and 'tailwind.config.cjs' exist at project root and 'src/index.css' contains the @tailwind directives.
